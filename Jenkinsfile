pipeline {
  agent any
  // any, none, label, node, docker, dockerfile, kubernetes
  tools {
    maven 'my_maven'
  }
  stages {
    stage('Example') {
      steps {
        echo 'Hello World'
        }
    }
  }
}

